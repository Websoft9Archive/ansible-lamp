{% if (init_db is defined ) and (init_db is not none) %}
{% for db_names,dbs in init_db.items() %}
{{db_names}} username:{{dbs.user}}
{{db_names}} password:{{dbs.password}}


{% endfor %}
{% endif %}
{% if (init_application is defined ) and (init_application.application_database_user is not none) %}
---For {{ init_application.application_name }} Installation Wizard---

database hostname:{{init_application.application_database_host | default('localhost',true)}}
database name:{{init_application.application_name}}
database username:{{init_application.application_database_user}}
database password:{{init_application.application_database_password}}
{% endif %}